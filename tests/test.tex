\documentclass{article}

\newcommand\logtime[1]{%
	#1\typeout{#1: \the\pdfelapsedtime}%
}

\typeout{pre usepackage: \the\pdfelapsedtime}
\usepackage
	{fractional-math}
\typeout{post usepackage: \the\pdfelapsedtime}

\fracKeys{
	number style = {\num{#1}}
}

\usepackage{siunitx}
\sisetup{locale = UK}

\typeout{pre begindoc: \the\pdfelapsedtime}
\begin{document} %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\logtime{number 1+halv} \hfill \fracFormat{1 1/2}

\logtime{number 1+halv reduce = 1.5} \hfill \fracFormat[reduce]{1 1/2}

\logtime{number 1+2halv reduce = 2} \hfill \fracFormat[reduce]{1 2/2}

\logtime{number 1+3halv reduce = 2.5} \hfill \fracFormat[reduce]{1 3/2}

math

\begingroup
	\fracKeys{add to variable=XYZ}
	\logtime{add to XYZ} \hfill \fracFormat{1 1/2}

	\logtime{add to XYZ} \hfill \fracFormat{1 1/2}

	\logtime{add to XYZ} \hfill \fracFormat{1 1/2}

	\logtime{result} \hfill \fracResult{XYZ}
	
	\logtime{result reduce} \hfill \fracResult[reduce]{XYZ}

	\fracKeys{subtract from variable=XYZ}

	\logtime{subtract from XYZ} \hfill \fracFormat{1 1/2}

	\logtime{result} \hfill \fracResult{XYZ}
	
	\logtime{result reduce} \hfill \fracResult[reduce]{XYZ}

	\logtime{subtract from XYZ} \hfill \fracFormat{1 1/2}

	\logtime{subtract from XYZ} \hfill \fracFormat{1 1/2}

	\logtime{subtract from XYZ} \hfill \fracFormat{ 1/2}

	\logtime{result} \hfill \fracResult{XYZ}
	
	\logtime{result reduce} \hfill \fracResult[reduce]{XYZ}

	\logtime{subtract from XYZ} \hfill \fracFormat{2 1/2}

	\logtime{subtract from XYZ} \hfill \fracFormat{1 1/2}

	\logtime{result} \hfill \fracResult{XYZ}
	
	\logtime{result reduce} \hfill \fracResult[reduce]{XYZ}

\endgroup

\logtime{init ABC = 0.333} \hfill \fracInit{ABC}{ 1/3}

\logtime{math ABC * 3} \hfill \fracMath{ABC}{*}{3}

\logtime{result ABC} \hfill \fracResult{ABC}

\logtime{result ABC reduce} \hfill \fracResult[reduce]{ABC}

\logtime{pre enddoc}
\end{document}